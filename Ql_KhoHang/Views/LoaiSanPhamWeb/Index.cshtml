@model List<Ql_KhoHang.Dtos.LoaiSanPhamWebDtos>

@{
    ViewData["Title"] = "LoaiSanPham";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="main-wrapper">
    <div class="page-wrapper">
        <div class="content container-fluid px-3 my-3">
            <div class="row">
                <h2>LoaiSanPham</h2>

                <div class="mb-3 d-flex justify-content-between">
                    <!-- Nút thêm mới danh mục -->
                    <a asp-action="Create" class="btn btn-primary">Add New Category</a>

                    <!-- Form tìm kiếm danh mục -->
                    <form asp-action="Search" method="get" class="form-inline d-flex">
                        <input type="text" name="keyword" class="form-control" placeholder="Search by keyword" />
                        <button type="submit" class="btn btn-secondary ml-2">Search</button>
                    </form>
                </div>

                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Image</th>
                            <th>Add Date</th>
                            <th>Update Date</th>
                            <th>Order</th>
                            <th>Link</th>
                            <th>Hidden</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                       @*  @foreach (var category in Model)
                        {
                            <tr>
                                <td>@category.CategoryId</td>
                                <td>@category.Name</td>
                                <td>@category.Description</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(category.Image))
                                    {
                                        <img src="@category.Image" alt="@category.Name" width="50" height="50" />
                                    }
                                </td>
                                <td>@category.AddDate?.ToString("yyyy-MM-dd")</td>
                                <td>@category.UpdateDate?.ToString("yyyy-MM-dd")</td>
                                <td>@category.Order</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(category.Link))
                                    {
                                        <a href="@category.Link" target="_blank">View Link</a>
                                    }
                                </td>
                                <td>@(category.Hide == true ? "Yes" : "No")</td>
                                <td>
                                    <!-- Nút hành động -->
                                    <a asp-action="Details" asp-route-id="@category.CategoryId" class="btn btn-info btn-sm">View</a>
                                    <form asp-action="Edit" asp-route-id="@category.CategoryId" asp-controller="Category" method="post" class="btn btn-warning btn-sm">Edit</form>
                                    <form asp-action="Delete" asp-route-id="@category.CategoryId" asp-controller="Category" method="post" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this category?');">Delete</form>
                                </td>
                            </tr>
                        } *@
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>

